# Copy to .env for local Docker Compose, or set these in your process/host (Kubernetes, Azure App Settings, etc.).
# ASP.NET Core maps nested config with double underscores: Section__Subsection__Key
# https://learn.microsoft.com/en-us/aspnet/core/fundamentals/configuration/

ASPNETCORE_ENVIRONMENT=Production

HumanConfiguration__px_app_id=
HumanConfiguration__px_cookie_secret=
HumanConfiguration__px_auth_token=
